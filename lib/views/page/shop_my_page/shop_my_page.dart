import 'package:flutter/material.dart';

class ShopMyPage extends StatelessWidget {
  const ShopMyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'ShopMyPage'
        ),
      ),
    );
  }
}
