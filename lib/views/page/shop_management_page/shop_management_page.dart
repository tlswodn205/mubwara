import 'package:flutter/material.dart';

class ShopManagementPage extends StatelessWidget {
  const ShopManagementPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          '샵 매니지먼트'
        ),
      ),
    );
  }
}
