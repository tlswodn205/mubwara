import 'package:flutter/material.dart';

class ShopAnalysisPage extends StatelessWidget {
  const ShopAnalysisPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('분석중'),
      ),
    );
  }
}
